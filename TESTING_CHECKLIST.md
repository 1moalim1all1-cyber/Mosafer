# قائمة اختبار مسافر | Mosafer - قبل أي إطلاق فعلي

> اختبر البنود دي بالترتيب على جهازين حقيقيين مختلفين (سائق وراكب) عشان
> تلاحظ التفاعل الحي بينهم (Real-time) بشكل صحيح.

## 1. الإعداد الأولي
- [ ] نشرت `firestore.rules` و `firestore.indexes.json` و Cloud Functions بنجاح
- [ ] فعّلت خطة Blaze في Firebase Console
- [ ] فعّلت Firebase Authentication → Email/Password من الـ Console
- [ ] أنشأت Upload Preset في Cloudinary بحالة Unsigned
- [ ] الفهارس المركبة كلها "Enabled" في Firestore Console (مش "Building")

## 2. التسجيل والدخول
- [ ] إنشاء حساب راكب جديد (تحقق من ظهور وثيقة في `users` و`wallets`)
- [ ] إنشاء حساب سائق جديد → لازم يتوجّه تلقائيًا لشاشة رفع المستندات
- [ ] تسجيل خروج ثم دخول تاني بنفس الحساب
- [ ] "نسيت كلمة المرور" → تحقق من وصول إيميل الاستعادة فعليًا
- [ ] محاولة دخول برقم غلط → تظهر رسالة خطأ عربية مفهومة

## 3. اعتماد السائق
- [ ] رفع الخمس مستندات + بيانات السيارة → تتحول الحالة لـ "pending"
- [ ] من Firebase Console، عدّل `verificationStatus` لـ `approved` يدويًا
  (أو استخدم `approveDriver` عبر Functions Shell) → الشاشة تتحدّث فورًا
  من غير ما تعمل Refresh (Stream حي)
- [ ] السائق الغير معتمد لا يقدر يوصل لشاشة إنشاء رحلة (الزرار مختفي)

## 4. إنشاء ونشر الرحلات
- [ ] سائق معتمد ينشئ رحلة عادية
- [ ] سائق معتمد (ذكر) ينشئ رحلة "راجع فاضي" → الشارة الخضرا تظهر صح
- [ ] سائقة معتمدة تنشئ رحلة "سيدات فقط" → الخيار ميظهرش للسائق الذكر أصلاً
- [ ] راكب ذكر يبحث → رحلة السيدات **لا تظهر نهائيًا** في نتائجه
- [ ] راكبة تفعّل فلتر "سيدات فقط" → تشوف الرحلة المخصصة

## 5. الحجز والدفع
- [ ] حجز بالدفع النقدي → الحالة "pending"، عدد المقاعد بينقص فورًا
- [ ] حجز بالمحفظة برصيد كافي → الخصم يحصل فورًا، `paymentStatus` = paid
- [ ] محاولة حجز بالمحفظة برصيد غير كافي → رسالة خطأ واضحة، الحجز ميتمّش
- [ ] حجزين من جهازين مختلفين على آخر مقعدين في نفس اللحظة → واحد بس ينجح
  (اختبار الـ Race Condition - جرّبها فعليًا بجهازين سوا)

## 6. رد السائق على الحجز
- [ ] قبول حجز → الراكب يوصله إشعار (In-app + Push لو التطبيق مقفول)
- [ ] رفض حجز مدفوع بالمحفظة → المقاعد ترجع + الفلوس ترجع لمحفظة الراكب فورًا
- [ ] رفض حجز نقدي → المقاعد ترجع بس مفيش حركة محفظة (لأن مفيش فلوس اتقبضت)

## 7. إنهاء الرحلة والأرباح
- [ ] السائق يضغط "إنهاء الرحلة" → كل الحجوزات المؤكدة تتحول لـ "completed"
- [ ] الحجوزات المدفوعة بالمحفظة → أرباحها (بعد خصم العمولة) تظهر في محفظة
      السائق فورًا، مع حركة "عمولة" منفصلة في السجل

## 8. المحادثة
- [ ] إرسال رسالة نصية من الراكب → تظهر فورًا عند السائق (وبالعكس)
- [ ] إرسال صورة → ترفع على Cloudinary وتظهر في المحادثة
- [ ] إرسال موقع → يظهر كإحداثيات في فقاعة الرسالة
- [ ] مؤشر "بيكتب دلوقتي..." يظهر فعليًا وقت الكتابة عند الطرف التاني
- [ ] فتح المحادثة يعلّم رسائل الطرف التاني كمقروءة (✓✓ تظهر عند المُرسل)

## 9. الإشعارات
- [ ] صلاحية الإشعارات بتتطلب فعليًا أول مرة يفتح التطبيق بعد تسجيل الدخول
- [ ] قفل التطبيق بالكامل ثم قبول حجز من جهاز تاني → يوصل Push حقيقي
      على شاشة القفل (ده أهم اختبار لكل منطق Phase 7)
- [ ] عداد الإشعارات غير المقروءة في الصفحة الرئيسية بيتحدّث لحظيًا

## 10. المحفظة
- [ ] طلب إيداع → يظهر في السجل بحالة "بانتظار الموافقة"، الرصيد ميتغيّرش
- [ ] محاولة تعديل الرصيد يدويًا من Firestore Console كمستخدم عادي (مش أدمن)
      → لازم تتفشل (تأكيد إن القواعد شغالة فعلًا)

## 11. اللغة والاتجاه
- [ ] تبديل اللغة للإنجليزي (لو فعّلته لاحقًا) → الاتجاه يتحول LTR تلقائيًا
- [ ] الوضع الداكن يشتغل صح على كل الشاشات من غير تباين ألوان غريب

## 12. حالات الحافة (Edge Cases)
- [ ] فصل النت أثناء عملية حجز → رسالة خطأ واضحة، مفيش بيانات ناقصة
- [ ] محاولة حجز رحلة اتلغت أو خلصت مقاعدها من نافذتين مختلفتين
- [ ] تسجيل دخول بحسابين مختلفين (راكب وسائق) على نفس الجهاز بالتبادل

---
لو أي بند فشل، ابعتلي رسالة الخطأ بالظبط (من `flutter logs` أو Firebase
Functions logs عبر `firebase functions:log`) وهساعدك تشخّصه.
